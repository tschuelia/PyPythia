name: Build and test PyPythia
run-name: Build and run the tests of the PyPythia python library.
on: [push]
jobs:
  Run-Pythia-Tests:
    runs-on: ubuntu-latest
    steps:
#      - name: Setup RAxML-NG
#        run: |
#          git clone --recursive https://github.com/amkozlov/raxml-ng.git
#          cd raxml-ng && mkdir build && cd build
#          cmake .. && make -j
      - name: Set RAxML-NG environment variable
        run: |
          cd .. && echo pwd && echo "raxmlng=test/bin/raxml-ng" >> $GITHUB_ENV
      - name: use the value
        run: |
          echo "${{ env.raxmlng }}"
#      - name: Check out repository code
#        uses: actions/checkout@v3
#      - name: Install Pythia and setup requirements
#        run: |
#          sudo pip install -e .
#          pip install pytest
#          rm tests/test_config.py
#          echo "RAXMLNG_COMMAND = '${{ env.raxmlng }}'" > tests/test_config.py
#      - name: Run Pythia tests
#        run: |
#          PYTHONPATH=. pytest
